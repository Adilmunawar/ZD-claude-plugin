# Command and agent reference

Generated by `scripts/gen-docs.py` from the plugin sources; CI fails if this file is stale.

## zaraat-dost

| Command | Purpose |
|---|---|
| `/zaraat-dost:doctor` | Check runtimes, GIS libraries, DB clients, installed modules, secrets hygiene, repo standards and toolkit version; print pass/fail with fixes. |
| `/zaraat-dost:help [module|command]` | List all toolkit commands and agents by module. Use when asked what the toolkit can do or how to run something. |
| `/zaraat-dost:setup` | Configure the current repository for the team: .claude/settings.json, .gitignore entries, CLAUDE.md. |
| `/zaraat-dost:standards` | Apply team repository standards (.editorconfig, .gitattributes, pre-commit, VS Code, PR template, code of conduct) with diffs before writing. |
| `/zaraat-dost:upgrade` | Update marketplace, bundle and every module; show changelog since the installed version; apply migration notes; re-run doctor. |
| `/zaraat-dost:workstation [--dry-run]` | Set up or verify an engineer's machine: Git, Node, Python GIS stack, GDAL, .NET SDK, GitHub CLI, VS Code extensions, Claude Code. |

Hooks: see `hooks/hooks.json` and `scripts/*.js`.

## zd-agis

| Agent | Model | Use for |
|---|---|---|
| `agis-engineer` | inherit | Builds and fixes the AGIS web dashboard: Next.js pages and API routes, Firebase, server-side Earth Engine, Pyodide workers, Leaflet tools, the Spaces inference backend. |

| Command | Purpose |
|---|---|
| `/zd-agis:audit` · forked | Security and performance audit of the AGIS dashboard into docs/AGIS-AUDIT.md. |
| `/zd-agis:new-tool <tool-name> <purpose>` | Scaffold a new AGIS dashboard tool: page, client component, API route, sidebar entry, worker stub. |

| Background skill (applied automatically) | When |
|---|---|
| `agis-architecture` | Layout and conventions of the AGIS Next.js dashboard: routes, component layers, workers, backends, env vars. _(activates on matching paths)_ |
| `cadastral-schema` | Mauza/Parcel Firestore model, size limits, geohash, geometry storage, rule constraints. _(activates on matching paths)_ |
| `gee-api-routes` | Server-side Earth Engine in Next.js API routes: init once, promisified evaluate, layers, palettes, tile URLs, validation, timeouts. _(activates on matching paths)_ |
| `inference-backend` | Flask inference backend on Hugging Face Spaces: job lifecycle, endpoints, telemetry, model loading, limits, proxy routes. _(activates on matching paths)_ |
| `pyodide-workers` | Browser-side GIS in Web Workers: Pyodide protocol, progress, memory limits, when to use pure JS. _(activates on matching paths)_ |

## zd-core

| Agent | Model | Use for |
|---|---|---|
| `stack-analyst` | inherit | Read-only map of an unfamiliar repository: stack, architecture diagram, data flow, entry points, risks, top-5 actions. Use when joining a project or before a large refactor. |

| Command | Purpose |
|---|---|
| `/zd-core:handoff [slug]` | Write a session hand-over document so a colleague or a fresh session can continue without the transcript. |
| `/zd-core:onboard [--force]` | Generate or refresh CLAUDE.md from the repository (stack, run commands, data layout, conventions). |
| `/zd-core:secrets-audit [path] [--history]` | Find credentials in the working tree and git history (keys, tokens, connection strings); walk through rotation and history purge. |

| Background skill (applied automatically) | When |
|---|---|
| `stack-detect` | Detect language, framework, database, spatial libraries and tile stack from repo files before DB, dashboard or deploy work. Python, .NET, Node/Next.js, Expo. |

Hooks: see `hooks/hooks.json` and `scripts/*.js`.

## zd-deploy

| Agent | Model | Use for |
|---|---|---|
| `release-engineer` | inherit | Deploys and operates services (Firebase App Hosting, Vercel, HF Spaces, VM/AWS, EAS) with pre-flight checks, staged rollout, verification and rollback notes. Never deploys to production without approval. |

| Command | Purpose |
|---|---|
| `/zd-deploy:aws-plan [service|all]` | AWS architecture, IaC skeleton, migration steps and cost estimate into docs/AWS-PLAN.md. |
| `/zd-deploy:dockerize <service-dir>` | Production Dockerfile and compose for Next.js, Flask/GDAL, .NET or pipeline workers. |
| `/zd-deploy:preflight <service>` | Pre-deployment checklist: build, tests, env vars by name, secrets audit, migrations, health, rollback, approval. |

| Background skill (applied automatically) | When |
|---|---|
| `deploy-profiles` | How each service runs locally and in the cloud (Firebase App Hosting, Vercel, Hugging Face Spaces, EAS, Docker, AWS) with commands, config, env sources, health checks, rollback. Apply to deploy, hosting, environment or AWS tasks. |

## zd-gee

| Command | Purpose |
|---|---|
| `/zd-gee:harvest-detect <timeseries.parquet> [crop]` | Detect harvest dates per parcel from NDVI (and S1) drops; produce a harvest layer and summary. |
| `/zd-gee:ndvi-timeseries <parcel-asset-or-file> <start> <end> [index]` | Per-parcel index time series from Sentinel-2, gap-filled and smoothed, exported as a table. |

| Background skill (applied automatically) | When |
|---|---|
| `gee-auth` | Earth Engine authentication patterns (service account via env, interactive, project id) and common auth errors. |
| `gee-export` | Fixes for Earth Engine limits: payload size, user memory, too many elements, timeouts, task queue, Drive quota. |
| `sentinel-composite` | Cloud-free Sentinel-2 and speckle-filtered Sentinel-1 composites and fused stacks for crop mapping. |

## zd-gis

| Agent | Model | Use for |
|---|---|---|
| `db-analyst` | inherit | Read-only database analyst for Postgres/PostGIS, SQL Server, MySQL, GeoPackage: schema, spatial columns, indexes, sizes, relationships, hotspots; writes docs/DATABASE.md with an ER diagram. |
| `geo-data-qa` | haiku | Read-only QA gate for shapefile/GeoJSON/GeoPackage/KML: CRS, validity, bounds, duplicates, attributes, slivers, overlaps. Use before delivery, load or publish. |
| `gis-dashboard-manager` | inherit | Engineer for spatial databases, ETL, map layers, tile services and dashboard bugs or performance. Python (Streamlit/Dash/FastAPI/Django), .NET (ASP.NET Core, EF Core + NetTopologySuite), JS map clients. Use proactively for GIS-stack work. |

| Command | Purpose |
|---|---|
| `/zd-gis:export-deliverable <layer-or-table> [client]` | Produce a client-ready shapefile and GeoJSON (clean, 4326, area_ha, QA-checked) from a layer or table. |
| `/zd-gis:new-layer <layer-name> [source]` | Publish a map layer end to end: table, display view, tile service, dashboard config, legend, smoke test. |
| `/zd-gis:qa-vector <file-or-table>` | Run the geo-data-qa checks on a vector file or table and print the report. |
| `/zd-gis:study-dashboard [path]` · forked | Audit an existing GIS dashboard (pages, layers, endpoints, freshness, performance, UX) into docs/DASHBOARD.md. |

| Background skill (applied automatically) | When |
|---|---|
| `pakistan-crs` | Which CRS to use for Punjab/Sindh (4326 storage, UTM 42N/43N for area) and how to reproject. Apply to area, distance, buffer, raster alignment or wrong-location bugs. |
| `postgis-conventions` | Spatial table rules: naming, SRID, indexes, area in UTM, ingest validation, safe migrations. PostGIS with SQL Server and GeoPackage equivalents. Apply to any SQL, migration or ORM model with geometry. |
| `study-db` | Read-only catalog queries for Postgres/PostGIS, SQL Server, MySQL, GeoPackage: spatial columns, indexes, sizes, FKs, slow spatial queries. Apply when documenting or diagnosing a database. |

## zd-ml

| Agent | Model | Use for |
|---|---|---|
| `seg-trainer` | inherit | Trains, fine-tunes and evaluates satellite segmentation models (UNet/HRNet/SegFormer, PyTorch, smp, timm, HF) with pre-flight checks, resumable checkpoints and model cards. |

| Command | Purpose |
|---|---|
| `/zd-ml:model-card <checkpoint-dir>` | Write an HF-compatible model card (data, classes, metrics, limits) next to a checkpoint. |

| Background skill (applied automatically) | When |
|---|---|
| `colab-ram-safe` | Patterns for RAM-limited notebooks: windowed reads, patch shards, progress files, Drive checkpoints. |
| `seg-preflight` | Ten-point checklist that catches common segmentation-pipeline bugs (checkpoint keys, shared dataset refs, split leakage, nodata, class balance). Apply before any training run or when predictions are garbage. |
| `train-template` | Structure for a PyTorch segmentation training script: resume, early stopping, cosine LR, AMP, full-state checkpoints, tiled inference. |

## zd-mobile

| Agent | Model | Use for |
|---|---|---|
| `mobile-engineer` | inherit | Works on the Expo / React Native farmer app: screens, RTL i18n, API client and contract, secure storage, on-device TFLite, MapLibre, EAS builds. |

| Command | Purpose |
|---|---|
| `/zd-mobile:i18n-parity [--fix]` | Find missing or unused string keys across language tables; add flagged placeholders; add the parity test. |
| `/zd-mobile:release-checklist [profile]` | Pre-release verification for the Expo app: typecheck, tests, bundle check, EAS profile, versions, permissions, model asset, OTA. |

| Background skill (applied automatically) | When |
|---|---|
| `api-contract` | Envelope and DTO contract between the Expo app and the .NET farmer API: success from body, verbatim field names, refresh, error mapping, parity checks. _(activates on matching paths)_ |
| `app-rules` | Non-negotiable rules for the farmer Expo app: RTL-safe layout, single text component, Latin digits, server-truth numbers, secure storage, signing parity. _(activates on matching paths)_ |

## zd-models

| Agent | Model | Use for |
|---|---|---|
| `pipeline-engineer` | inherit | Runs, debugs and extends the cadastral and land-use pipeline: tile fetch, boundary inference, vectorisation, road subtraction, Earth Engine features, classification, sub-parcelling, SAM2. Knows stage order, layout and failure modes. |

| Command | Purpose |
|---|---|
| `/zd-models:script-to-package <script.py>` | Turn a constants-at-top pipeline script into a configured, resumable, tested CLI. |

| Background skill (applied automatically) | When |
|---|---|
| `boundary-inference` | Chunked low-RAM boundary model inference over an AOI from XYZ tiles: tile cache, patch scan, Hanning blending, hysteresis, merged COG. _(activates on matching paths)_ |
| `landuse-classify` | Train or run the XGBoost land-use classifier: classes, T-minus alignment, model bundle, unified parcel/sub-parcel inference, predictions.gpkg. _(activates on matching paths)_ |
| `sam2-boundaries` | Zero-shot field boundaries with SAM2: tiled inference, seam stitching, cleanup, GeoJSON, alignment. _(activates on matching paths)_ |
| `spectral-features` | Per-parcel Sentinel-1/2 feature matrix from Earth Engine: 15-day composites, band and index set, chunked reduceRegions, batch parquet, resume. _(activates on matching paths)_ |
| `sub-parcelling` | Split large or irregular parcels into analysis units before feature extraction. _(activates on matching paths)_ |

## zd-ops

| Command | Purpose |
|---|---|
| `/zd-ops:incident <one-line summary>` | Run an incident: severity, roles, reversible mitigation per service, status updates, evidence, hand-off to postmortem. |
| `/zd-ops:oncall-handoff [week]` | Weekly on-call hand-over: incidents, alerts, deploys, expiring credentials, quotas, risks. |
| `/zd-ops:postmortem <incident-doc>` | Blameless postmortem from an incident doc with action items, owners and dates. |
| `/zd-ops:runbook <service>` | Service runbook with verified commands: health, restart, rollback, secrets rotation, scaling, escalation. |

| Background skill (applied automatically) | When |
|---|---|
| `observability` | Logging, metrics and alert conventions per service. Apply when adding logging, telemetry or alerts. |

## zd-quality

| Agent | Model | Use for |
|---|---|---|
| `code-reviewer` | inherit | Read-only pull-request reviewer across TypeScript, Python, .NET and Expo: verdict, blocking issues, should-fix, nits, missing tests. Use on 'review this' or before merging. |

| Command | Purpose |
|---|---|
| `/zd-quality:adr <title>` | Write an architecture decision record into docs/adr/. |
| `/zd-quality:changelog [version]` | Generate a CHANGELOG section from conventional commits since the last tag. |
| `/zd-quality:pr-description [base-branch]` | Write a pull request description from the branch diff. |
| `/zd-quality:tech-debt [path]` · forked | Measured technical-debt audit into docs/TECH-DEBT.md. |

| Background skill (applied automatically) | When |
|---|---|
| `conventional-commits` | Commit message and branch naming convention with product scopes. Apply when writing commits or creating branches. |
| `review-standards` | Merge criteria per stack (TypeScript/Next.js, Python, .NET, Expo) plus cross-cutting rules on secrets, contracts, migrations, tests, CRS. Apply during code review or before a PR. |

## zd-reports

| Command | Purpose |
|---|---|
| `/zd-reports:deliverable-memo <deliverable-dir>` | One-page hand-over note for a geospatial deliverable: contents, method, QA, how to open, caveats. |
| `/zd-reports:harvest-report <harvest.gpkg> [docx|pptx]` | Client harvest progress report (docx/pptx) from harvest-detect outputs. |
| `/zd-reports:layer-metadata <layer-file>` | Metadata sheet (JSON + Markdown) for a vector or raster layer before publishing or delivery. |

## zd-security

| Agent | Model | Use for |
|---|---|---|
| `security-reviewer` | inherit | Read-only security reviewer for the dashboard, inference backend, .NET API and Expo app; ranked findings with evidence and fixes. Use for 'is this secure' or pre-release reviews. |

| Command | Purpose |
|---|---|
| `/zd-security:dependency-audit [path]` · forked | Vulnerability and licence audit for npm, pip and dotnet with an upgrade plan. |
| `/zd-security:harden-repo` | Apply SECURITY.md, Dependabot, secrets-scan workflow, CODEOWNERS and .gitignore baseline; list GitHub settings to enable. |

| Background skill (applied automatically) | When |
|---|---|
| `security-review` | Application security checklist: auth and session, authorisation and Firestore rules, input validation, secrets, PII exposure, dependencies, infra and CI. Apply during any security review. |

## zd-usage

| Command | Purpose |
|---|---|
| `/zd-usage:budget [weekly-tokens|show|clear]` | Set or show the weekly token budget that triggers a session-start warning at 80% and 100%. |
| `/zd-usage:report [today|week|month|all] [--by project|model|day|week] [--export <dir>]` | Claude usage report from local transcripts: tokens and estimated cost by project, model, day or week for today, this week, this month or a date range; CSV export for the team. |
| `/zd-usage:team-report <shared-dir>` | Merge teammates' exported usage CSVs from a shared folder into one table per user and week. |

| Background skill (applied automatically) | When |
|---|---|
| `usage-hygiene` | Practices that keep Claude Code sessions cheap and focused: scope, context management, model choice, forked commands, compaction, when to start a new session. Apply when the user asks how to use Claude efficiently or a session grows long. |

Hooks: see `hooks/hooks.json` and `scripts/*.js`.

## zd-vector

| Agent | Model | Use for |
|---|---|---|
| `vector-engineer` | inherit | Turns raster predictions into clean parcel vectors: tiling, topology repair, straightening, road removal, gap analysis, QA. Use for any raster-to-parcel task. |

| Command | Purpose |
|---|---|
| `/zd-vector:gap-analysis <mapped.gpkg> <aoi-or-previous.gpkg>` | Compare a mapped parcel layer with an AOI or previous version: missing layer, coverage summary, added/removed/changed parcels. |

| Background skill (applied automatically) | When |
|---|---|
| `raster-to-polygons` | RAM-safe raster to polygon conversion for large rasters: tiling with overlap, components, seam merge, min-area filter, resume. |
| `road-subtract` | Subtract OSM/Overture road, canal and rail buffers from parcels; fix gap-fill edge bleeding. |
| `straighten-edges` | Remove staircase edges from raster-derived parcels with a junction graph and deviation test, preserving shared boundaries. |
| `topology-repair` | Fix invalid geometries, overlaps, gaps, slivers and mixed types in parcel layers while keeping shared edges. Apply after polygonisation, simplification or merge. |

