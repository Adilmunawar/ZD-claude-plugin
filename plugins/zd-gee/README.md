# zd-gee

| Component | Purpose |
|---|---|
| `gee-auth`, `gee-export`, `sentinel-composite` (auto) | Auth patterns, export limit workarounds, S1/S2 composites |
| `/zd-gee:ndvi-timeseries` | Per-parcel index time series, smoothed |
| `/zd-gee:harvest-detect` | Harvest dates per parcel + summary |
