# zd-mobile

| Component | Type | Purpose |
|---|---|---|
| `mobile-engineer` | agent | Screens, i18n, API client, secure storage, TFLite, EAS |
| `app-rules` | skill | RTL, `Txt`, digits, server-truth numbers, secure store, signing parity |
| `api-contract` | skill | Envelope, DTO parity with the .NET API, refresh, error mapping |
| `/zd-mobile:i18n-parity` | command | Missing/unused keys across language tables |
| `/zd-mobile:release-checklist` | command | Pre-release verification |
